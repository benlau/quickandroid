import QtQuick 2.0
import QuickAndroid 0.1
import ".."

MenuDropdownPanelHoloLight {
}

/*
Item {

    implicitHeight: image.border.bottom + image.border.top
    implicitWidth : image.border.left + image.border.right

    width : 128
    height : 121

    BorderImage {
        id: image
        source: A.resolve("../drawable/menu_dropdown_panel_holo_light.png")
        anchors.fill: parent
        border.left: 10 * A.dp;
        border.top: 10 * A.dp
        border.right: 10 * A.dp;
        border.bottom: 10 * A.dp
    }
}
*/
